<%= form_for(solution) do |f| %>
  <% if solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(solution.errors.count, "error") %> prohibited this solution from being saved:</h2>

      <ul>
      <% solution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :problem_id %>
    <%= f.collection_select :problem_id, problems, :id, :name %>
    
  </div>

  <div class="field">
    <%= f.label :solutionFile %>
    <%= f.file_field :solutionFile %>
  </div>

  <div class="field">
    <%= f.label :language %>
    <%= f.select :language, Solution.languages.keys %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
